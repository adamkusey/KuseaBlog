@model IList<KuseaBlog.Core.Objects.Tag>
 
<div class="tags sidebar">
  <h3>Tags</h3>
 
  @if (Model.Count > 0)
  {
    foreach (var tag in Model)
    {
      @Html.TagLink(tag) 
      <span>&nbsp;</span>
    }
  }
  else
  {
    <p>No tags found!</p>
  }
</div>