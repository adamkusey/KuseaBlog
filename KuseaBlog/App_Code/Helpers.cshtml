@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, 
    IList<KuseaBlog.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">
        @KuseaBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)
    </div>
  }
}